# --- Secrets & Credentials (CRITICAL) ---
.env
.env.*
!.env.example
!.env.template
# AWS/Cloud keys om de skulle råka hamna här
.aws/
secrets/

# --- Dependency Management (uv) ---
# Vi ignorerar miljön, men INTE uv.lock (den är vår "truth")
.venv/
venv/
ENV/
env/

# --- Python Core & Build Artifacts ---
__pycache__/
*.py[cod]
*$py.class
# Distribution / Packaging (om vi bygger en wheel fil senare)
dist/
build/
*.egg-info/
.eggs/

# --- Code Quality & Testing (The Guardrails) ---
# Ruff cache (gör linter snabbare lokalt, men behövs ej i git)
.ruff_cache/
# Mypy cache (type checking history)
.mypy_cache/
.dmypy.json
# Pytest cache
.pytest_cache/
# Coverage reports (när vi kör pytest --cov)
.coverage
htmlcov/
coverage.xml

# --- IDE & Editor Junk ---
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
.idea/
*.swp
*.swo

# --- Logs & Temp ---
*.log
*.tmp
*.bak
*.old

# --- Data & Local Storage (SoC: Data lives in DB/Storage, not Git) ---
# Ignorera faktiska datafiler så vi inte committar kunddata eller stora filer
*.csv
*.tsv
*.parquet
*.avro
*.json
*.xml
# ...men tillåt specifika config/exempel-filer om vi vill
!config/*.json
!examples/*.csv
!seeds/*.csv

# --- Local Databases ---
*.sqlite
*.sqlite3
*.db
*.duckdb
*.wal
*.journal

# --- Docker & Containers ---
# Bind mounts för databaser (viktigt att inte committa databasens fysiska filer)
pgdata/
postgres_data/
mysql_data/
mongo_data/
docker/.env